pipeline {
  agent { dockerfile true 
          label 'linux-1'
        }
  stages {
    stage('Test') {
      steps {
        sh '''
          node --version
          git --version
          curl --version
        '''
      }
    }
  }
}
